enum RenderModeEnum {
  Shaded,
  ShadedWithoutEdges,
  Unshaded,
  UnshadedWithHiddenEdges,
  Wireframe,
  Translucent,
}

const TOOL_BAR_HEIGHT = 40

const DEMO_PANEL_WIDTH = 240

export { DEMO_PANEL_WIDTH, RenderModeEnum, TOOL_BAR_HEIGHT }
